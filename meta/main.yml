galaxy_info:
  author: Mariusz Słowiński
  description: Manage NFS server exports

  license: GPL-2.0-or-later

  min_ansible_version: 2.10

   platforms:
     - name: Ubuntu
       versions:
         - all
     - name: GenericLinux
       versions:
         - any

  galaxy_tags:
    - nfs
    - exports

dependencies:
  - role: mario_slowinski.service
    service_pkgs: "{{ nfs_pkgs }}"
    service_firewalld: "{{ nfs_firewalld }}"
